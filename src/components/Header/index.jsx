import styles from "./styles.module.css"

const Header = () => {
    return(
        <div className={`${styles.container}`}>
            <h1>Pokémon</h1>
        </div>
    )
}

export default Header